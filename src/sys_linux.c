#include "lib/linquake.h"
#include "lib/quakedef.h"


int32_t main(int32_t com_argc, char* com_argv[]) {
    /*
     * To make the game work as similar as posible to Quake, limit the number
     * of arguments to MAX_NUM_ARGVS
     */
    //if (com_argc > 1 && com_argc < MAX_NUM_ARGVS + 1) {
    //    int32_t test = COM_CheckParam(com_argc, com_argv, "-setalpha");
    //    if (test != 0) {
    //        printf("%d\n", Q_atoi(com_argv[test + 1]));
    //    }
    //}
    return 0;
}

